# -----------------------------------------------------------------------------
# Frontend + shared application settings (exposed to Vite via VITE_* prefix)
# -----------------------------------------------------------------------------

# Display name injected into <title> and other UX surfaces.
VITE_APP_TITLE=DJ Danny Hectic B

# Logo URL used for favicons and metadata; can be relative or absolute.
VITE_APP_LOGO=https://placehold.co/128x128/E1E7EF/1F2937?text=App

# Manus App ID issued from the Manus dashboard. Must match the OAuth project.
VITE_APP_ID=manus-app-id

# Hosted Manus OAuth portal URL (e.g., https://oauth.manus.im).
VITE_OAUTH_PORTAL_URL=https://oauth.manus.im

# Forge proxy credentials for browser-side Google Maps access.
VITE_FRONTEND_FORGE_API_KEY=forge-public-api-key
VITE_FRONTEND_FORGE_API_URL=https://forge.manus.im

# Optional analytics instrumentation.
VITE_ANALYTICS_ENDPOINT=https://analytics.manus.im
VITE_ANALYTICS_WEBSITE_ID=00000000-0000-0000-0000-000000000000

# -----------------------------------------------------------------------------
# Server-only secrets (loaded via dotenv before bootstrapping Express/TRPC)
# -----------------------------------------------------------------------------

# Same Manus App ID the frontend uses; required for OAuth + session validation.
VITE_APP_ID=manus-app-id

# Symmetric key for signing/validating user session JWTs.
JWT_SECRET=replace-with-long-random-string

# MySQL connection string consumed by Drizzle + runtime data access.
DATABASE_URL=mysql://username:password@host:3306/dj_danny

# Manus OAuth server base URL (exchange tokens + fetch user profiles).
OAUTH_SERVER_URL=https://oauth.manus.im

# OpenID of the project owner; grants adminProcedure access automatically.
OWNER_OPEN_ID=owner-open-id

# Forge service credentials for AI, storage proxy, maps proxy, notifications.
BUILT_IN_FORGE_API_URL=https://forge.manus.im
BUILT_IN_FORGE_API_KEY=forge-server-api-key

# Optional: override server port (defaults to 3000) and environment mode.
PORT=3000
NODE_ENV=development

# -----------------------------------------------------------------------------
# CI / local developer hints
# -----------------------------------------------------------------------------

# Copy this file to .env and fill in real values before running:
#   pnpm install
#   pnpm dev
# Drizzle CLI also relies on DATABASE_URL when generating/migrating schemas.
